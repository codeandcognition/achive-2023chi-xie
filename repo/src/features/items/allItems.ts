import stringTimes from "./data/string_times.json"
import makeChocolate from './data/make_chocolate.json'
import centeredAverage from './data/centered_average.json'

let allItems = {
  'a': stringTimes,
  'b': makeChocolate,
  'c': centeredAverage
}

export default allItems
